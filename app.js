const app=document.getElementById("app");let currentUser=null,currentPage="home",currentGame=null,currentTab="Game Lines",slip=[];
function qs(s){return document.querySelector(s)}
function renderLogin(){app.innerHTML=`<div class="header"><div class="h1">${STATE.title}</div><div class="sub">${STATE.subtitle}</div></div><div class="wrap"><div class="loginCard"><div class="sectionTitle">Login</div><div class="small">Enter your PIN to access the sportsbook.</div><label class="small" style="display:block;margin-top:14px">User<select id="loginUser">${STATE.users.map(u=>`<option value="${u.name}">${u.name}${u.isAdmin?" (Admin)":""}</option>`).join("")}</select></label><label class="small" style="display:block;margin-top:12px">PIN<input id="loginPin" type="password" inputmode="numeric" maxlength="4" placeholder="4-digit PIN"></label><button class="primary" id="loginBtn" style="margin-top:14px;width:100%">Login</button></div></div>`;qs("#loginBtn").onclick=()=>{const user=STATE.users.find(u=>u.name===qs("#loginUser").value);const pin=qs("#loginPin").value.trim();if(!user||pin!==user.pin){alert("Wrong PIN");return;}currentUser=user;renderApp();};}
function selectedClass(label){return slip.some(s=>s.label===label)?"selected":""}
function toggleSlip(label,odds){const idx=slip.findIndex(s=>s.label===label);if(idx>=0)slip.splice(idx,1);else slip.push({label,odds});renderApp();}
function slipOddsDisplay(){if(slip.length<2)return"Need at least 2 legs";const pseudo=100+slip.length*125;return`+${pseudo}`}
function renderHeader(){return`<div class="header"><div class="h1">${STATE.title}</div><div class="sub">${STATE.subtitle} • Week ${STATE.week}</div></div>`}
function renderHome(){return`<div class="wrap"><div class="card"><div class="sectionTitle">Welcome, ${currentUser.name}</div><div class="small">Available Bankroll: <strong>$${currentUser.available}</strong></div></div>${STATE.games.map(g=>`<div class="boardCard"><div class="gameTheme" style="background:linear-gradient(90deg, ${g.theme[0]}, ${g.theme[1]})"></div><div class="boardTop"><div class="matchup"><img src="${g.awayLogo}" alt=""><span>${g.title}</span></div><button class="ghost" onclick="openGame('${g.id}')">Open</button></div><div class="marketGrid"><div class="marketHdr"></div><div class="marketHdr">Spread</div><div class="marketHdr">Moneyline</div><div class="marketHdr">Total</div><div class="teamCell"><img src="${g.awayLogo}" alt=""><span>${g.away}</span></div><button class="oddsBtn ${selectedClass(g.away+" "+g.board.away.spread)}" onclick="event.stopPropagation();toggleSlip('${g.away} ${g.board.away.spread.replaceAll("\n"," ")}','')"><span class="line1">${g.board.away.spread}</span></button><button class="oddsBtn ${selectedClass(g.away+" ML "+g.board.away.ml)}" onclick="event.stopPropagation();toggleSlip('${g.away} ML ${g.board.away.ml}','')"><span class="line1">${g.board.away.ml}</span></button><button class="oddsBtn ${selectedClass(g.away+" total "+g.board.away.total)}" onclick="event.stopPropagation();toggleSlip('${g.away} ${g.board.away.total.replaceAll("\n"," ")}','')"><span class="line1">${g.board.away.total}</span></button><div class="teamCell">${g.homeLogo?`<img src="${g.homeLogo}" alt="">`:'<div style="width:28px;height:28px;border-radius:50%;background:#4b5563"></div>'}<span>${g.home}</span></div><button class="oddsBtn ${selectedClass(g.home+" "+g.board.home.spread)}" onclick="event.stopPropagation();toggleSlip('${g.home} ${g.board.home.spread.replaceAll("\n"," ")}','')"><span class="line1">${g.board.home.spread}</span></button><button class="oddsBtn ${selectedClass(g.home+" ML "+g.board.home.ml)}" onclick="event.stopPropagation();toggleSlip('${g.home} ML ${g.board.home.ml}','')"><span class="line1">${g.board.home.ml}</span></button><button class="oddsBtn ${selectedClass(g.home+" total "+g.board.home.total)}" onclick="event.stopPropagation();toggleSlip('${g.home} ${g.board.home.total.replaceAll("\n"," ")}','')"><span class="line1">${g.board.home.total}</span></button></div></div>`).join("")}</div>`}
function openGame(id){currentGame=STATE.games.find(g=>g.id===id);currentTab="Game Lines";currentPage="game";renderApp();}
function renderGame(){const g=currentGame;return`<div class="wrap"><div class="card"><div class="flex" style="justify-content:space-between"><button class="ghost" onclick="currentPage='home';renderApp()">← Back</button><div class="small">${g.title}</div></div><div class="sectionTitle" style="margin-top:10px">${g.title}</div><div class="tabBar">${Object.keys(g.tabs).map(tab=>`<button class="${tab===currentTab?'active':''}" onclick="currentTab='${tab}';renderApp()">${tab}</button>`).join("")}</div>${(g.tabs[currentTab]||[]).map(item=>`<div class="leg"><div class="legName">${item[0]}</div><button class="oddsBtn ${selectedClass(item[0]+" "+item[1])}" style="min-width:100px" onclick="toggleSlip('${item[0]} ${item[1]}','${item[1]}')"><span class="line1">${item[1]}</span></button></div>`).join("")}</div></div>`}
function renderMyBets(){const mine=STATE.bets.filter(b=>b.user===currentUser.name);return`<div class="wrap"><div class="card"><div class="sectionTitle">My Bets</div><div class="small">Clean ticket cards instead of a messy table.</div></div>${mine.map(b=>`<div class="ticket"><div class="ticketTop"><div><div style="font-weight:900;font-size:20px">${b.id}</div><div class="small">${b.week} • ${b.type}</div></div><div class="badge ${b.status}">${b.status}</div></div><div class="small" style="margin-top:8px">Odds: ${b.odds} • Stake: $${b.stake} • Potential Win: ${b.payout}</div><ul class="legsList">${b.legs.map(l=>`<li>${l}</li>`).join("")}</ul></div>`).join("")}</div>`}
function renderBankroll(){return`<div class="wrap"><div class="card"><div class="sectionTitle">Full League Bankroll</div><table class="bankTable"><thead><tr><th>User</th><th>Start</th><th>Wagered</th><th>Available</th><th>Open Bets</th></tr></thead><tbody>${STATE.users.map(u=>`<tr><td>${u.name}</td><td>$${u.start}</td><td>$${u.wagered}</td><td>$${u.available}</td><td>${u.openBets}</td></tr>`).join("")}</tbody></table></div><div class="card"><div class="sectionTitle">League Rules</div><ul class="ruleList small"><li>Starting Bankroll: $${STATE.rules.startBankroll}</li><li>Season Max: $${STATE.rules.seasonMax}</li><li>Weekly Max Exposure: $${STATE.rules.weeklyMax}</li><li>Max Parlay Stake: $${STATE.rules.maxParlayStake}</li><li>Minimum Legs: ${STATE.rules.minLegs}</li><li>Max Season Legs: ${STATE.rules.maxSeasonLegs}</li><li>Max Weekly Legs: ${STATE.rules.maxWeeklyLegs}</li></ul></div></div>`}
function renderAdmin(){if(!currentUser.isAdmin){return`<div class="wrap"><div class="card"><div class="sectionTitle">Admin</div><div class="small">Only Tuane can access this page.</div></div></div>`;}return`<div class="wrap"><div class="card"><div class="sectionTitle">Admin Panel</div><div class="small">Grade bets, adjust bankroll, and update odds later from these files.</div></div><div class="card"><div class="sectionTitle">Open Bets</div>${STATE.bets.filter(b=>b.status==='OPEN').map(b=>`<div class="ticket" style="margin-bottom:10px"><div class="ticketTop"><div><strong>${b.id}</strong> • ${b.user}</div><div class="badge OPEN">OPEN</div></div><div class="small">Stake: $${b.stake} • Odds: ${b.odds}</div><ul class="legsList">${b.legs.map(l=>`<li>${l}</li>`).join("")}</ul><div class="flex" style="margin-top:10px"><button class="primary" onclick="alert('Grade logic can be updated in app.js later.')">Mark Win</button><button class="ghost" onclick="alert('Grade logic can be updated in app.js later.')">Mark Loss</button></div></div>`).join("")}</div></div>`}
function renderSlip(){if(!slip.length)return'';return`<div class="betSlip"><div class="flex" style="justify-content:space-between"><div style="font-weight:900">Bet Slip (${slip.length})</div><button class="ghost" onclick="slip=[];renderApp()">Clear</button></div>${slip.map(s=>`<div class="betSlipItem">${s.label}</div>`).join("")}<div style="margin-top:12px" class="small">Parlay Odds: <strong>${slipOddsDisplay()}</strong></div><input id="stake" type="number" min="25" max="${STATE.rules.maxParlayStake}" placeholder="Stake (min $25)"><div class="small" style="margin-top:8px">Min 2 legs • Max ${STATE.rules.maxWeeklyLegs} weekly • Max $${STATE.rules.maxParlayStake} per parlay</div><button class="primary" style="width:100%;margin-top:10px" onclick="placeBet()">Place Bet</button></div>`}
function placeBet(){if(slip.length<STATE.rules.minLegs){alert('Minimum 2 legs');return;}if(slip.length>STATE.rules.maxWeeklyLegs){alert('Maximum 5 weekly legs');return;}const stake=Number(qs('#stake')?.value||0);if(stake<25){alert('Minimum stake is $25');return;}if(stake>STATE.rules.maxParlayStake){alert('Max per parlay is $150');return;}if(stake>currentUser.available){alert('Not enough available bankroll');return;}alert('Bet placed. You can track it in My Bets.');slip=[];renderApp();}
function renderNav(){const items=[['home','Home'],['game','Games'],['bets','My Bets'],['bank','Bankroll'],['admin','Admin']];return`<div class="bottomNav">${items.map(([key,label])=>`<button class="navBtn ${currentPage===key?'active':''}" onclick="${key==='game'?(currentGame?`currentPage='game';renderApp()`:`currentPage='home';renderApp()`):`currentPage='${key}';renderApp()`}">${label}</button>`).join("")}</div>`}
function renderApp(){if(!currentUser){renderLogin();return;}let body='';if(currentPage==='home')body=renderHome();if(currentPage==='game')body=currentGame?renderGame():renderHome();if(currentPage==='bets')body=renderMyBets();if(currentPage==='bank')body=renderBankroll();if(currentPage==='admin')body=renderAdmin();app.innerHTML=renderHeader()+body+renderSlip()+renderNav();}
renderApp();